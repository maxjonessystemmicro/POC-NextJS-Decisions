/** @type {import('tailwindcss').Config} */
module.exports = {
  theme: {
    extend: {
      colors: {
        background: '#f4f4f4', // Light gray background
        primary: '#00704a', // Green accents
        secondary: '#000000', // Black text/icons
        accent: '#FFD700', // Yellow/gold accents
        cardBg: '#ffffff', // White for card backgrounds
      },
      borderRadius: {
        large: '16px',
      },
    },
  },
  plugins: [],
};
